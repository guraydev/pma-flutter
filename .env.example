# Example Environment Variables for Flutter Frontend

# Backend GraphQL API Endpoint
# Make sure this matches your NestJS backend configuration
# (protocol, host, port, /graphql endpoint)
# For local development, if backend is on port 3001:
GRAPHQL_API_URL=http://localhost:3001/graphql